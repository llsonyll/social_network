import { INotification } from './../../types/index';
import express, {Request,Response} from 'express';
import passport from 'passport';
import { Notification } from '../../mongoose';


const router = express.Router()











export default router